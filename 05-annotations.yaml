apiVersion: v1
kind: Pod
metadata:
  name: pod2
  annotations:
    imageregistry: "https://hub.docker.com/"
  labels:
    namespace: expense
    proj: expense
spec:
  containers:
  - name: nginx
    image: nginx